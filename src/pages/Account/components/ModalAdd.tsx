import React from 'react';

interface ModalAddProps {
  setOpen: React.Dispatch<React.SetStateAction<boolean>>;
}
const ModalAdd = ({}: ModalAddProps) => {
  return <div>ModalAdd</div>;
};

export default ModalAdd;
