import React from 'react';

interface ModalUpdateProps {
  setOpen: React.Dispatch<React.SetStateAction<boolean>>;
  data: any;
}

const ModalUpdate = ({}: ModalUpdateProps) => {
  return <div>ModalUpdate</div>;
};

export default ModalUpdate;
